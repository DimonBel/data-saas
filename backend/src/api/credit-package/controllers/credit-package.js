'use strict';

/**
 * credit-package controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::credit-package.credit-package');
